// Include modules
const fs = require("fs");
const {
    // Import functions, objects, classes
    l,
    API,
    Message
} = require("./lib/lib");



const listen = new Message();
listen.OnMessage();